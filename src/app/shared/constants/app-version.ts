/**
 * App version shown in the footer. Update manually on release — package.json's
 * version is zeroed by the build script, so it can't be the source of truth.
 */
export const APP_VERSION = '1.17.2';
